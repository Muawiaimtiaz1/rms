# Database Mode (Set to postgres to enable migration)
DB_CLIENT=postgres

# PostgreSQL Connection Details
PGHOST=localhost
PGPORT=5432
PGDATABASE=rms_pos
PGUSER=postgres
PGPASSWORD=1234

# SSL Configuration (Uncomment if using a cloud DB like Supabase or Neon)
# PGSSLMODE=require
# PGSSL_REJECT_UNAUTHORIZED=false

# App Settings
PORT=4000
SESSION_SECRET=pos-super-secret-key-2024
